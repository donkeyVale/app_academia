# Data model (alto nivel)

Este documento describe el modelo de datos **a nivel funcional** (no SQL raw). La fuente de verdad del esquema está en:

- `supabase/migrations/` (si existe)
- `supabase/schema.sql` (baseline actual)

## Entidades principales

## Reglas y constraints relevantes

## Relaciones (FK) principales

## Vistas / RPC / funciones relevantes

## Notas de RLS y seguridad
